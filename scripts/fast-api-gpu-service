[Unit]
Description=Uvicorn GPU API
After=network.target

[Service]
User=cloufish
WorkingDirectory=/usr/bin
ExecStart=/usr/bin/python3 -m uvicorn fast-api-gpu:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target

# Create this file inside /etc/systemd/system
